<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select setmeal.* , category.name as categoryName
        from setmeal left join category on setmeal.category_id = category.id
        <where>
            <if test="name != null and name != ''">setmeal.name like concat('%',#{name},'%')</if>
            <if test="status != null">and setmeal.status = #{status}</if>
            <if test="categoryId != null">and setmeal.category_id = #{categoryId}</if>
        </where>
        order by setmeal.create_time desc
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal(name, price, description, image, create_time, update_time, create_user,
                            update_user, status,category_id)
        values (#{name}, #{price}, #{description}, #{image}, #{createTime}, #{updateTime}, #{createUser},
                #{updateUser}, #{status},#{categoryId})
    </insert>

    <update id="update">
        update setmeal
        <set>
            <if test="categoryId != null">category_id = #{categoryId}</if>
            <if test="name != null and name != ''">,name = #{name}</if>
            <if test="price != null">,price = #{price}</if>
            <if test="status != null">,status =#{status}</if>
            <if test="description != null and description !=''">,description =#{description}</if>
            <if test="image != null and image != ''">,image = #{image}</if>
            <if test="createTime != null ">,create_time = #{createTime}</if>
            <if test="updateTime != null">,update_time = #{updateTime}</if>
            <if test="createUser != null">,create_user = #{createUser}</if>
            <if test="updateUser != null">,update_user = #{updateUser}</if>
        </set>
        where id = #{id}
    </update>

</mapper>
